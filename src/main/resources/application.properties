spring.application.name=student-management-system

spring.profiles.active=${SPRING_PROFILES_ACTIVE:dev}

jwt.secret=${JWT_SECRET}
jwt.expiration-ms=${JWT_DURATION}

admin.email=${ADMIN_EMAIL}
admin.password=${ADMIN_PASSWORD}

spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME}
spring.mail.password=${MAIL_PASSWORD}

spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
management.health.mail.enabled=true

spring.jpa.open-in-view=false
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=false

spring.data.web.pageable.default-page-size=20
spring.data.web.pageable.max-page-size=100


spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=5MB
spring.servlet.multipart.max-request-size=10MB

management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=when-authorized

spring.cache.type=caffeine


app.email.from-name=STUDENT MANAGEMENT SYSTEM (SMS)

# AWS S3
aws.s3.bucket=${AWS_BUCKET_NAME:sms-student-files-ilanie}
aws.s3.region=${AWS_REGION:eu-north-1}
aws.access.key=${AWS_ACCESS_KEY:AKIAQQ5MKFVHDI4MEHM5}
aws.secret.key=${AWS_SECRET_KEY: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}
